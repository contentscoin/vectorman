{
  "name": "@perfectvector/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3000",
    "//build": "The engine is a workspace dependency resolved through its main field, which points at dist. Webpack cannot resolve it from a fresh clone until it is compiled, so both builds compile it first. This lives here rather than only in the root scripts because a host may invoke this package's build directly, which is what Vercel does.",
    "build": "pnpm --filter @perfectvector/core build && next build",
    "build:static": "pnpm --filter @perfectvector/core build && PV_STATIC_EXPORT=1 next build",
    "start": "next start -p 3000",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@perfectvector/core": "workspace:*",
    "next": "^15.5.4",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@types/react": "^19.1.9",
    "@types/react-dom": "^19.1.7",
    "autoprefixer": "^10.4.20",
    "playwright-core": "^1.62.0",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.6.3"
  }
}
